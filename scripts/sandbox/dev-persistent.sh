#!/usr/bin/env bash
#
# dev-persistent.sh — Phase 2 placeholder.
#
# Manages long-lived persistent sandbox workspaces. Gated behind the
# SANDBOX_ENABLE_PERSISTENT feature flag (see lib/sandbox/feature-flags.ts).
# This script will be implemented in Phase 2 once persistent workspaces
# are introduced; it exists now so the CLI surface is discoverable.
#
set -euo pipefail

echo "dev-persistent.sh is a Phase 2 placeholder."
echo "Persistent workspaces are gated behind SANDBOX_ENABLE_PERSISTENT."
echo "This script will be implemented once the flag is enabled."
exit 0
