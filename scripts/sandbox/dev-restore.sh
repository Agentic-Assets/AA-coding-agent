#!/usr/bin/env bash
#
# dev-restore.sh — Phase 1 placeholder.
#
# Restores a sandbox from a previously built snapshot. Gated behind the
# SANDBOX_ENABLE_SNAPSHOTS feature flag (see lib/sandbox/feature-flags.ts).
# This script will be fleshed out in Phase 1; it exists now so the
# Phase 0 reviewer can see the intended surface area.
#
set -euo pipefail

echo "dev-restore.sh is a Phase 1 placeholder."
echo "Restore-from-snapshot is gated behind SANDBOX_ENABLE_SNAPSHOTS."
echo "This script will be implemented once the flag is enabled."
exit 0
